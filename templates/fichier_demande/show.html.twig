{% extends 'base.html.twig' %}

{% block title %}FichierDemande{% endblock %}

{% block body %}
    <h1>FichierDemande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fichier_demande.id }}</td>
            </tr>
            <tr>
                <th>Nom_fichier</th>
                <td>{{ fichier_demande.nomFichier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fichier_demande_index') }}">back to list</a>

    <a href="{{ path('app_fichier_demande_edit', {'id': fichier_demande.id}) }}">edit</a>

    {{ include('fichier_demande/_delete_form.html.twig') }}
{% endblock %}
